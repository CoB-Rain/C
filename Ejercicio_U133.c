/*U133. Desarrollar un programa que permita cargar en un array “n” números 
enteros y los muestre en pantalla en orden ascendente utilizando el método de 
burbujeo optimizado.*/

#include <stdio.h>
#include <stdlib.h>
#include <windows.h>
#include <locale.h>
#include <string.h>
#include <stdbool.h> 

int main()
{
    setlocale(LC_ALL,"spanish");
    system("cls");
    int x=0,y=0,z=0,aux=0,fila=0;
    int ne[x];
    for(y=0;y<x;y++)
    {
        ne[x]==0;
    }
    for(y=0;y<x;y++)
    {
        for(z=0;z<y;z--)
        {
            
        }
    }
    system("pause");
    return 0;
}